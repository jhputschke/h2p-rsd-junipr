# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
.mypy_cache/
.ruff_cache/
.pytest_cache/

# Scratch notebook copies. The generated notebooks are executed from a COPY at the repo
# root so `_REPO` resolves correctly, and those copies carry MBs of outputs that have no
# business in git -- they have twice been committed by accident.
_*.ipynb

# Runs / data / artifacts
runs/
*.root
!cpp/test_data/jets.root
!cpp/test_data/jets_aux.root
*.pt
cache/
data_cache/

# C++ build
cpp/build/
*.o
*.a

# OS / editors
.DS_Store
.vscode/
.idea/
*.code-workspace
