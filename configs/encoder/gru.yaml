name: gru
emb_dim: 32
hidden_dim: 64
num_layers: 1
bidirectional: true
dropout: 0.1

# aux conditioning: [] == off (byte-identical). docs/PLAN_Input.md
#   e.g. encoder.aux_features="[ln_mg_pt,nsec,ln_pt]"
aux_features: []
